# Environment Variables Template
# Copy this file to .env and fill in the actual values
# NEVER commit the actual .env file to version control

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
NODE_ENV=development
PORT=8000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# Get this from: https://cloud.mongodb.com → Database → Connect → Connect your application
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/maintenance-tracker?retryWrites=true&w=majority

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT Secret Key - MUST be a strong random string (minimum 64 characters)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_64_characters_long_generated_randomly

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Frontend URL for CORS configuration
# Development: http://localhost:3500
# Production: https://your-app.onrender.com
FRONTEND_URL=http://localhost:3500

# =============================================================================
# EMAIL CONFIGURATION (Optional - for notifications)
# =============================================================================
# Leave empty to use mock email service (development)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Gmail App Password Setup:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use the generated 16-character password here

# =============================================================================
# AWS CONFIGURATION (Optional - only if using AWS services)
# =============================================================================
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# DynamoDB Configuration (if using AWS DynamoDB)
DYNAMODB_TABLE_PREFIX=maintenance_

# S3 Configuration (if using AWS S3 for file uploads)
S3_BUCKET_NAME=your-s3-bucket-name

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
# 
# For Render.com deployment:
# 1. Set NODE_ENV=production
# 2. Generate new JWT_SECRET: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Update MONGODB_URI with your MongoDB Atlas connection string
# 4. Update FRONTEND_URL with your actual frontend URL
# 5. Add these variables in Render dashboard (Settings → Environment)
# 6. NEVER commit actual credentials to git
#
# For MongoDB Atlas:
# 1. Create free cluster at mongodb.com/cloud/atlas
# 2. Create database user with password
# 3. Whitelist IP addresses (0.0.0.0/0 for Render, or specific IPs)
# 4. Get connection string and replace <password> with actual password
#
# Security Checklist:
# ✓ JWT_SECRET is at least 64 characters long
# ✓ MongoDB password is strong (16+ chars, mixed case, numbers, symbols)
# ✓ .env file is in .gitignore
# ✓ No credentials committed to version control
# ✓ MongoDB IP whitelist configured
# ✓ HTTPS enabled in production (automatic on Render)
#
